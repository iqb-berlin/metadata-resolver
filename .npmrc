# NPM Configuration

# Default registry (change if using a private registry)
# registry=https://registry.npmjs.org/

# Force public access by default (overrides package.json if needed)
# access=public

# Save exact versions in package.json
save-exact=true
